#!/usr/env/Rscript --vanilla 

# Install CRAN Packages
install.packages(c(
                   "devtools",
                   "googlesheets",
                   "geosphere",
                   "leaflet",
                   "rlang",
                   "webshot",
                   "knitr"
                   )
                )